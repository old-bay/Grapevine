const superHeroes: string[] = [];
const heroPower: number[] = [];
const isArtificial: Array<boolean> = [];

type User = {
    name: string;
    power: number;
}

const users: User[] = [];

superHeroes.push("Spidey");
heroPower.push(10000);
isArtificial.push(false);

users.push({name: "Spooder", power: 1000});


const MLModels: number[][] = [
    [255, 255, 255],
    [255, 255, 255]
];

export {}

